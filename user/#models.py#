from django.db import models

# Create your models here.

class Tag(models.Model):
    title = models.CharField(max_length = 20)
    members = models.ManyToManyField(User, through = 'Label')
    
    def __str__(self):
        return self.title

class Label(models.Model):
    
